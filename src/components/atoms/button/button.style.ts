import { styled } from "@stitches/react";

export const StyledButton = styled("button", {
	backgroundColor: "$accent",
	border: "none",
	borderRadius: "4px",
	color: "White",
	padding: "0.5rem 4rem",
});
